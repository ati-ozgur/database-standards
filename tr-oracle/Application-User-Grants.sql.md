```sql
SELECT OWNER, TABLE_NAME ,

'GRANT SELECT, INSERT, UPDATE, DELETE ON ' || OWNER || '.' || TABLE_NAME || ' TO module_name_user;' AS GRANTS

FROM DBA_TABLES 
WHERE OWNER IN ('SCHEMA1','TT_SCHEMA1','SCHEMA2','TT_SCHEMA2')
ORDER BY OWNER,TABLE_NAME 
;
```